/**
 * this dir contains the ModelSpecifcation for each domain model. The implementation is in the domain folder.
 * export your model config to deploy,e.g.
 * ```js
 * export * from './MyDomainModel'
 * ```
 */
